# This stuff is just for prepare local dev env

You should run basenine DB using script or manualy before start api server
And build(if need) frontend 
Run this script `./scripts/build-ui.sh` in the project root directory

You can automate this process. It's up to you!
